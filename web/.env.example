VITE_API_URL=http://localhost:3001
# Google Maps JavaScript API key — browser-exposed by design, but MUST be restricted in
# Google Cloud Console to this app's actual origins (HTTP referrer restriction) and to only the
# Maps JavaScript API, Places API, and Geocoding API. Never leave it unrestricted.
VITE_GOOGLE_MAPS_API_KEY=
